# Production provisioning qualification — 2026-09-04

Issue: [#185](https://github.com/dokor/ade-control-plane/issues/185).
Related gates: [#189](https://github.com/dokor/ade-control-plane/issues/189),
[#153](https://github.com/dokor/ade-control-plane/issues/153).

## Result: incomplete, not a production pass

The operator authorized `dokor/ade-control-plane` and authenticated Dashboard
access. No Raspberry terminal, checkout deletion, credential replacement or PR
merge was used. Authentication material is intentionally absent from this report.

## Observations

- Secure implementation: [PR #188](https://github.com/dokor/ade-control-plane/pull/188),
  main revision `214cfafa8fd16dbcab0a2770a1be653165361ce4`.
- [Main CI](https://github.com/dokor/ade-control-plane/actions/runs/33861808660)
  passed. This proves code validation, not production provisioning.
- [Production deployment](https://github.com/dokor/ade-control-plane/actions/runs/33861906786)
  succeeded for the exact revision above; the job completed at 10:35:01 UTC.
- [Authorized project](https://ade.lelouet.fr/projects/51aae106-13c3-4e3e-a3f5-fb29d9974773)
  was already registered and enabled. At the 10:27 UTC setup check, repository
  files, declared skill paths, labels and GitHub App read access passed; runner
  capabilities were `unknown`, with no observed checkout revision. Correlation:
  `5b52667c-09a2-4c7a-b561-723d93d778d4`.
- At the 10:33:53 UTC project refresh, initialization remained blocked. The audit
  timeline contained both checkout-ready and checkout-failed events, including a
  failure at 10:32:23 UTC. A ready event alone does not establish a fresh clone,
  successful initialization or the deployed image revision.
- Existing failed task
  [9d046acb-e7fb-4939-9258-cbdaf682157d](https://ade.lelouet.fr/tasks/9d046acb-e7fb-4939-9258-cbdaf682157d)
  reported `GIT_CLONE_FAILED` before this qualification. It is historical evidence,
  not a new run against #188.
- After successful deployment, one initialization was admitted at 10:35:42.803 UTC:
  [task 4a905210-410e-4a75-8e32-ea3414dd8609](https://ade.lelouet.fr/tasks/4a905210-410e-4a75-8e32-ea3414dd8609).
  It remained `PENDING`, with no branch, commit or PR. At 10:37:07 UTC the Overview
  reported worker `waiting-quota`, provider capacity `blocked`, 95% used and a
  reset at 13:43:21 UTC. No quota policy was changed; the task remains queued and
  may start when capacity permits. This is not evidence of an SSH failure or pass.
- The complete onboarding checkpoint report and continuation procedure are in
  [PR #193](https://github.com/dokor/ade-control-plane/pull/193).

## Dashboard diagnostic correction

The project timeline displayed only “Project checkout failed — Deferred”, hiding
the bounded remediation metadata emitted by the worker. It now includes the
classified reason, fixed GitHub host and sanitized, bounded action. Raw stderr,
checkout paths and arbitrary metadata are not rendered by this addition.

The regression test verifies diagnostic visibility and exclusion of raw fields
and secret-shaped values. Local Dashboard tests (146) and `pnpm typecheck` passed.
These tests do not satisfy the production acceptance criteria.

## Resume without Raspberry terminal

1. The deployment criterion is verified above. A healthy HTTP endpoint alone
   would not have been sufficient evidence of the worker version.
2. Wait for quota policy to permit execution, then review the existing task and
   provisioning diagnostics.
   Do not create duplicate initialization tasks or bypass SSH host verification.
3. Follow the already-created initialization task through the Dashboard. Record
   its safe failure code or resulting runner capability snapshot and checkout SHA.
4. Establish that the successful provisioning actually used an absent checkout.
   If that cannot be established through retained evidence, leave this criterion
   unverified; do not delete the current checkout to manufacture a fresh import.
5. If SSH identity authorization fails, an authorized operator must repair the
   mounted Git credentials separately. Never paste keys into issues or logs.
6. Continue the #189 onboarding/delivery qualification and link its outcome to
   #153. Leave all real-run criteria open until backed by correlated evidence.
